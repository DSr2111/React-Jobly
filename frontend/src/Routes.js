import { Route, Routes, Navigate } from 'react-router-dom';

function AppRoutes() {
  return (
    <Routes>
      <Route path="/" />
      <Route path="/" />
      <Route path="/" />
    </Routes>
  );
}

export default AppRoutes;
