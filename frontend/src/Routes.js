import { Route, Routes, Navigate } from 'react-router-dom';

function Routes() {
  return (
    <Routes>
      <Route path="/" />
      <Route path="/" />
      <Route path="/" />
    </Routes>
  );
}
